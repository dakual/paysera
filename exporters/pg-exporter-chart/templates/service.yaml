apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.name }}
  ports:
    - name: {{ .Values.name }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
